const myObject = {
    js: 'javascript',
    cpp: 'c++',
    rb: 'ruby',
    swift: "swift by apple"
}
    for (const key of myObjectbject) {
        //console.log(`${key} shorycut is for ${myObject[key]}`);
        
        }


        
// Variables and Data Types (10 Questions )

// Declare a variable using let and log its value.

let a = 12;
console.log(a);

// Create a constant to store the value of PI and log it.

const pi = Math.PI;
console.log(pi);


// Reassign a value to a variable declared with let and log the result.

let a = 12;
a = 32;
console.log(a)

// Check the type of null and log it.

log a = null;
console.log(typeof a); 


// Create a variable with a number as a string (e.g., "25") and log its type/

 var a = "254";
 console.log(typeof a);

// Use typeof to check the type of a boolean variable.


var a = true;
console.log(typeof a);


// Create three variables of types string, number and boolen, and log their values.

let aa = "hello", bb = 12, cc=false;
console.log(aa,bb,cc);

// Declare a variable without assigning a value. Log it's type.

var a;
console.log(typeof a);

// Create a variable with undefined with undefined and log its type.

const a = [1,2,3,4];
a.pop();





// Use const to create and array. Try reassigning the array and observe the error.

const myArray = [1, 2, 3];
myArray = [4, 5, 6]; // This will throw an error